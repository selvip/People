<% content_for :jumbotron do %>
	<h1>Add MotorCycle to Garage</h1>
<% end %>
<%= form_for([@person, @garage]) do |f| %>
	<%= f.collection_select(:motor_cycle_id, find_no_owner_motor, :id, :name) %>
	<%= f.submit "Submit", class: "btn btn-default" %>
<% end %>
<br /><br /><br />